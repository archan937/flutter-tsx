// DO NOT EDIT — generated by `fsx define`
// Utility functions that map to Dart SDK statics or single-call plugin APIs.

/** Open the system file picker and return the selected file path. */
export function pickFile(options?: { extensions?: string[]; allowMultiple?: boolean }): Promise<{ path: string; name: string } | null> {
  // Runtime stub — transpiler rewrites to Dart
  return undefined as never;
}

/** Load a bundled app asset (text, JSON) by its asset path. */
export function loadAsset(path: string): Promise<string> {
  // Runtime stub — transpiler rewrites to Dart
  return undefined as never;
}

/** Return the path to the app documents directory. */
export function appDir(): Promise<string> {
  // Runtime stub — transpiler rewrites to Dart
  return undefined as never;
}

/** Return the path to the temporary directory for scratch files. */
export function tempDir(): Promise<string> {
  // Runtime stub — transpiler rewrites to Dart
  return undefined as never;
}

/** Open a URL in the system browser or another external app. */
export function launchUrl(url: string, externalApp?: boolean): Promise<void> {
  // Runtime stub — transpiler rewrites to Dart
  return undefined as never;
}

/** Make HTTP requests — a familiar fetch-style API over the http package. */
export function fetch(url: string, options?: { method?: 'GET' | 'POST' | 'PUT' | 'DELETE'; body?: string; headers?: Record<string, string> }): Promise<{ ok: boolean; status: number; body: string; json: unknown }> {
  // Runtime stub — transpiler rewrites to Dart
  return undefined as never;
}

/** Open the OS share sheet to share text, URLs, or files. */
export function share(text: string, subject?: string): Promise<void> {
  // Runtime stub — transpiler rewrites to Dart
  return undefined as never;
}

export const hapticFeedback = {
  /** Trigger a light haptic impact (iOS) / tactile feedback (Android). */
  light(): Promise<void> {
    // Runtime stub — transpiler rewrites to Dart
    return undefined as never;
  },
  /** Trigger a medium haptic impact. */
  medium(): Promise<void> {
    // Runtime stub — transpiler rewrites to Dart
    return undefined as never;
  },
  /** Trigger a heavy haptic impact. */
  heavy(): Promise<void> {
    // Runtime stub — transpiler rewrites to Dart
    return undefined as never;
  },
  /** Trigger a standard vibration. */
  vibrate(): Promise<void> {
    // Runtime stub — transpiler rewrites to Dart
    return undefined as never;
  },
};

export const clipboard = {
  /** Copy text to the clipboard. */
  copy(text: string): Promise<void> {
    // Runtime stub — transpiler rewrites to Dart
    return undefined as never;
  },
  /** Read the current clipboard contents. */
  paste(): Promise<string | null> {
    // Runtime stub — transpiler rewrites to Dart
    return undefined as never;
  },
};

export const systemChrome = {
  /** Lock the app to portrait or landscape orientation. */
  setOrientation(orientation: 'portrait' | 'landscape' | 'any'): Promise<void> {
    // Runtime stub — transpiler rewrites to Dart
    return undefined as never;
  },
  /** Set the status bar background color. */
  setStatusBarColor(color: string): void {
    // Runtime stub — transpiler rewrites to Dart
    return undefined as never;
  },
};
